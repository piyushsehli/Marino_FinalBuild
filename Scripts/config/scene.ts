module config {
    export enum Scene {
        START,
        PLAY,
        END,
        SETTING,
        INFORMATION,
        PLAYLEVEL1,
        PLAYLEVEL2,
        LEVEL2,
        LEVEL3,
        RULES
    }
}